version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run:
          name: Set Custom String as Environment Variable
          command: |
            export CUSTOM_STRING=$(cat custom_string.txt)
            
      - run:
          name: Run Your Script
          command: node .circleci/index.js
            
workflows:
  version: 2
  build_and_run:
    jobs:
      - build


