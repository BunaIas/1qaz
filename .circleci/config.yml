version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest-browsers

    steps:
      - checkout

      - run:
          name: Install Puppeteer
          command: |
            npm install puppeteer
            # or if you prefer yarn:
            # yarn add puppeteer

      - run:
          name: Run Your Script
          command: node .circleci/2index.js


